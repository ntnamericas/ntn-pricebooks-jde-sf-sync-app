<?xml version="1.0" encoding="UTF-8"?>

<mule xmlns:os="http://www.mulesoft.org/schema/mule/os" xmlns:ee="http://www.mulesoft.org/schema/mule/ee/core"
	xmlns="http://www.mulesoft.org/schema/mule/core"
	xmlns:doc="http://www.mulesoft.org/schema/mule/documentation" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://www.mulesoft.org/schema/mule/core http://www.mulesoft.org/schema/mule/core/current/mule.xsd
http://www.mulesoft.org/schema/mule/ee/core http://www.mulesoft.org/schema/mule/ee/core/current/mule-ee.xsd
http://www.mulesoft.org/schema/mule/os http://www.mulesoft.org/schema/mule/os/current/mule-os.xsd">
	<sub-flow name="redrive-pricebook-updateSub_Flow" doc:id="739a73d5-1efe-4c34-b96c-20eae924073e" >
		<ee:transform doc:name="pbjobrun" doc:id="95ec1a3c-54ac-4fe8-be94-04273749998a" >
			<ee:message >
			</ee:message>
			<ee:variables >
				<ee:set-variable variableName="pbJobRun" ><![CDATA[%dw 2.0
import * from dw::core::Strings

// Function to extract the year from the date
fun year(date) =
  (date as DateTime).year
  
fun C_value(date) =
  floor((year(date) - 1900) / 100)

// Function to extract the day of the year from the date
fun dayOfYear(date) =
  (date as DateTime).dayOfYear

// Function to extract time from DateTime as a string, remove milliseconds and timezone, and select first 6 digits
fun extractTime(date) =
  do {
    var dateTimeStr = (date as String)
    var timePart = (dateTimeStr splitBy "T")[1] // Extract the time part after 'T'
    var timeWithoutMilliseconds = (timePart splitBy "\\.")[0] // Remove milliseconds
    var timeWithoutTimezone = (timeWithoutMilliseconds splitBy "\\+")[0] // Remove timezone
    var cleanTime = (timeWithoutTimezone replace ":" with "") // Remove colons
    ---
    cleanTime[0 to 5] // Select only the first 6 characters (HHmmss)
  }

// Function to compute the Julian date and time
fun JulianDate(date) =
  {
    // Julian date calculation: full year + day of year padded to 3 digits
    date: C_value(date) ++(year(date) as String)[2 to 3] ++ leftPad(dayOfYear(date), 3, "0"),
    
    // Julian time calculation: extract and format time
    time: extractTime(date)
  }

// Main output as JSON
output application/json  
---
JulianDate(now() as DateTime >>  "America/New_York" )]]></ee:set-variable>
				<ee:set-variable variableName="pricecode" ><![CDATA[%dw 2.0
import * from dw::core::Strings
output text/plain
---
substringBefore((substringAfter(payload.pricecode replace "," with "','" , "(")), ")")]]></ee:set-variable>
			</ee:variables>
		</ee:transform>
		<logger level="INFO" doc:name="jobRun logger" doc:id="bcf77b91-97e6-4802-bd2c-e1395e314ee4" message='#[%dw 2.0&#10;output application/java&#10;---&#10;"&#10;CurrentJobRunDetails : " ++ vars.pbJobRun.^raw]' category="${log.category}" />
		<ee:transform doc:name="pbupdate-delta query" doc:id="efdd9246-80b9-48a6-b97c-7c2d13da3139" >
			<ee:message >
				<ee:set-payload ><![CDATA[%dw 2.0
output text/plain
---
readUrl("classpath://queryScript/sqlScript-delta-pbUpdate-consolidated-$(p('mule.env')).dwl","text/plain")]]></ee:set-payload>
			</ee:message>
		</ee:transform>
		<logger level="INFO" doc:name="Logger" doc:id="985a105b-5643-47d5-b13f-e852eafbdfab" message='#["Calling \"update-pricebooks-subflow\" "]' category="${log.category}" />
		<flow-ref doc:name="update-pricebooksFlow" doc:id="66dc5255-4e17-4c2d-8ee6-b7b6831d4402" name="update-pricebooks-subflow" />
		<os:store doc:name="Store current job run details" doc:id="9e95a9ce-27b7-47c5-81c1-a870c80f10a7" key="previousPbJobRun" objectStore="Object_store_Previous_pb_Job_Run" >
			<os:value ><![CDATA[#[vars.pbJobRun]]]></os:value>
		</os:store>
		<ee:transform doc:name="Response" doc:id="f063c0f9-5a60-403b-8e78-281e37ac74cc" >
			<ee:message >
				<ee:set-payload ><![CDATA[%dw 2.0
output application/json
---
{
    correlationId: vars.correlationId,
    pricecode: vars.pricecode,
    setResponse: vars.setResponse,
    batchResults: payload pluck ((value) -> do {
        var item = if (value.exceptionPayload != null) value.exceptionPayload else value.payload
        ---
        {
            creationTime: item.creationTime,
            recordCount: item.recordCount,
            ownerJobName: item.ownerJobName,
            status: item.status
        }
    })
}]]></ee:set-payload>
			</ee:message>
		</ee:transform>
	</sub-flow>
	<sub-flow name="redrive-pricebook-dateTime-updateSub_Flow" doc:id="92ff4577-0b54-4919-8410-92b1dd16b106" >
		<ee:transform doc:name="pbjobrun" doc:id="c786b39f-4a39-474a-984a-5ab9388a0977" >
			<ee:message >
			</ee:message>
			<ee:variables >
				<ee:set-variable variableName="pbJobRun" ><![CDATA[%dw 2.0
import * from dw::core::Strings

// Function to extract the year from the date
fun year(date) =
  (date as DateTime).year
  
fun C_value(date) =
  floor((year(date) - 1900) / 100)

// Function to extract the day of the year from the date
fun dayOfYear(date) =
  (date as DateTime).dayOfYear

// Function to extract time from DateTime as a string, remove milliseconds and timezone, and select first 6 digits
fun extractTime(date) =
  do {
    var dateTimeStr = (date as String)
    var timePart = (dateTimeStr splitBy "T")[1] // Extract the time part after 'T'
    var timeWithoutMilliseconds = (timePart splitBy "\\.")[0] // Remove milliseconds
    var timeWithoutTimezone = (timeWithoutMilliseconds splitBy "\\+")[0] // Remove timezone
    var cleanTime = (timeWithoutTimezone replace ":" with "") // Remove colons
    ---
    cleanTime[0 to 5] // Select only the first 6 characters (HHmmss)
  }

// Function to compute the Julian date and time
fun JulianDate(date) =
  {
    // Julian date calculation: full year + day of year padded to 3 digits
    date: C_value(date) ++(year(date) as String)[2 to 3] ++ leftPad(dayOfYear(date), 3, "0"),
    
    // Julian time calculation: extract and format time
    time: extractTime(date)
  }

// Main output as JSON
output application/json  
---
JulianDate(now() as DateTime >>  "America/New_York" )]]></ee:set-variable>
				<ee:set-variable variableName="pricecode" ><![CDATA[%dw 2.0
import * from dw::core::Strings
output text/plain
---
substringBefore((substringAfter(payload.pricecode replace "," with "','" , "(")), ")")]]></ee:set-variable>
				<ee:set-variable variableName="previousPbJobRun" ><![CDATA[%dw 2.0
output application/json
---
payload mapObject ((value, key, index) -> {
 (key): value
 })]]></ee:set-variable>
			
</ee:variables>
		</ee:transform>
		<logger level="INFO" doc:name="jobRun logger" doc:id="0a5afcf8-d504-450e-a2fc-69b920b5805f" message='#[%dw 2.0&#10;output application/java&#10;---&#10;"&#10;CurrentJobRunDetails : " ++ vars.pbJobRun.^raw]' category="${log.category}" />
		<ee:transform doc:name="pbupdate-delta query" doc:id="f658334e-020f-485b-a170-d8d248754e39" >
			<ee:message >
				<ee:set-payload ><![CDATA[%dw 2.0
output text/plain
---
readUrl("classpath://queryScript/sqlScript-pbUpdate-consolidated-$(p('mule.env')).dwl","text/plain")]]></ee:set-payload>
			</ee:message>
		</ee:transform>
		<logger level="INFO" doc:name="Logger" doc:id="9aacdbbe-7948-4d2b-a0e1-7638854398ad" message='#["Calling \"update-pricebooks-subflow\" "]' category="${log.category}" />
		<flow-ref doc:name="update-pricebooksFlow" doc:id="9145843d-8548-44af-9286-ab515a112c95" name="update-pricebooks-subflow" />
		<os:store doc:name="Store current job run details" doc:id="7130cfa5-cb77-4666-a7e9-0549c1e091aa" key="previousPbJobRun" objectStore="Object_store_Previous_pb_Job_Run" >
			<os:value ><![CDATA[#[vars.pbJobRun]]]></os:value>
		</os:store>
		<ee:transform doc:name="Response" doc:id="3d4e0d6b-869d-4cf8-960b-41994d3b5b7c" >
			<ee:message >
				<ee:set-payload ><![CDATA[%dw 2.0
output application/json
---
{
    correlationId: vars.correlationId,
    pricecode: vars.pricecode,
    setResponse: vars.setResponse,
    batchResults:
        if (isEmpty(payload) or (sizeOf(payload)==0))
            ["No Data Found for this Run"]
        else
            payload pluck ((value) -> do {
                var item = if (value.exceptionPayload != null) 
                value.exceptionPayload else value.payload
                ---
                {
                    creationTime: item.creationTime,
                    recordCount: item.recordCount,
                    ownerJobName: item.ownerJobName,
                    status: item.status
                }
            })
}]]></ee:set-payload>
			</ee:message>
		</ee:transform>
	</sub-flow>
</mule>
